{
  "name": "digital-tattoo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "DIGITAL TATTOO — offline in-browser baton-pass art with WebLLM (inference only)",
  "scripts": {
    "fetch-model": "node scripts/fetch-model.mjs",
    "verify-models": "node scripts/verify-models.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite preview --host",
    "prepare-publish": "npm install && npm run fetch-model && npm run verify-models && npm run build && npm run verify-models -- dist"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "0.2.84"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
